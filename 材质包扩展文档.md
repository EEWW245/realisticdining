# 扩展

一个食物扩展由定义、模型、动画和材质组成：

```text
assets/realisticdining/
├── definitions/
│   └── example_food.json
├── geo/
│   └── example_food.geo.json
├── animations/
│   └── example_food.animation.json
├── sounds/
└── textures/item/
    └── example_food.png
```

`definitions/example_food.json` 示例：

```json
{
  "item": "examplemod:example_food",
  "mode": "pickup",
  "invisible": ["exampelBone"],
  "sounds": {
    "动画音乐关键帧名": "realisticdining:eat"
  }
}
```

- `item`：要替换渲染的物品 ID，任意模组的物品均可。
- `mode`：持物模式，`static` 拿到物品立即显示模型 + 程序化晃动；`pickup` 拿到物品自动播放「拿起」动画并定格在持物姿态。
- `invisible`：待机（持物）阶段隐藏的 bone 名称。隐藏父 bone 时其子 bone 也一同隐藏，进食动画播放期间恢复显示。
- `sounds`：将动画音效关键帧名称映射到游戏声音 ID，`realisticdining:eat` 对应 `realisticdining/sounds/eat.ogg`。

模型、动画、材质文件名必须与定义文件名一致。例如定义文件是 `example_food.json`，则应使用：

```text
geo/example_food.geo.json
animations/example_food.animation.json
textures/item/example_food.png
```

模型中的手臂骨骼名写 `rightarm` / `leftarm`

一个动画文件里要准备几个分动画，取决于 `mode`：

| `mode` | 需要的动画 | 说明 |
|--------|-----------|------|
| `static` | 1 个：`eat` | 拿到即显示模型，U 键播放 `eat` |
| `pickup` | 2 个：`pickup` + `eat` | `pickup` 拿起到持物，U 键播放 `eat` |

- 动画名："eat"，pickup模式写"pickup"，"eat"。
- 动画结束时，在时间线加入 动画效果-指令-脚本:finished

按 U 键播放进食动画。可参考仓库内的两个材质包。
