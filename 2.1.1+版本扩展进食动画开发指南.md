# Realistic Dining 附属开发指南

> **版本**：API v2.1（对应模组 v2.1.3+）
> **适用平台**：Fabric 1.20.1 / Forge 1.20.1 / Fabric 1.21.1 / NeoForge 1.21.1
> **作者**：Realistic Dining 模组作者

本指南面向希望为 Realistic Dining 开发附属（Addon）的模组作者。通过本模组提供的公共 API，为模组添加自定义的饮用/食用动画。{具有双模型，单模型API}

> **v2.1.3 新增**：Pickup/Putdown 动画（方案 B，第十三章）—— 物品进入主手时自动播放"拿起"动画并定格持物，按 U 键播放"食用"动画，切出主手时播放"放下"反向动画。
>
> 
>  本文档的大部分内容以及全部代码由Ai生成，作者是代码小白，仅供参考



---

## 一、能做什么 / 不能做什么

### ✅ 你可以做到的

1. **添加新的饮料/食物动画**：例如辣子鸡、烤鱼、汤碗、蛋糕等任何第一人称食用动画
2. **自定义动画时长**：1~30 秒任意时长
3. **自定义音效时间轴**：在动画的任意时间点触发音效，支持单次播放和循环播放
4. **复用主模组的模型/动画**：附属可以只改时长和音效，复用主模组的 geo/animation 文件
5. **完全自定义模型**：使用你自己的 BlockBench 模型和动画



--
## 二、前置依赖

附属的 `fabric.mod.json` / `neoforge.mods.toml` / `mods.toml` 必须声明依赖 Realistic Dining：

### Fabric 1.21.1 / 1.20.1（fabric.mod.json）

```json
{
  "depends": {
    "minecraft": ">=1.21",
    "fabricloader": ">=0.16",
    "realisticdining": ">=2.0.5"
  }
}
```

### NeoForge 1.21.1（neoforge.mods.toml）

```toml
[[dependencies.myaddon]]
modId = "realisticdining"
type = "required"
versionRange = "[2.0.5,)"
side = "BOTH"
```

### Forge 1.20.1（mods.toml）

```toml
[[dependencies.myaddon]]
modId = "realisticdining"
mandatory = true
versionRange = "[2.0.5,)"
ordering = "AFTER"
side = "BOTH"
```

---

## 三、核心概念：骨骼命名约定（**必须遵守**）

你的 BlockBench 模型必须严格遵循以下命名规则：

### 玩家手臂骨骼

| 骨骼名 | 含义 | UV 布局 |
|---|---|---|
| `Left Arm` | 玩家左臂 | 玩家皮肤 64×64 标准布局，左臂起始 (32, 48) |
| `Right Arm` | 玩家右臂 | 玩家皮肤 64×64 标准布局，右臂起始 (40, 16) |
| `Right_Arm2` | 玩家右臂第二层 | 玩家皮肤 64×64 标准布局，右臂起始 (40, 16) |

**关键**：这三根骨骼名的 UV 必须映射到**玩家标准皮肤布局**（64×64 皮肤文件的对应区域）。建议在 BlockBench 中使用 **"Java Entity / Player"** 模板新建模型，自带正确 UV。

### 食物/容器骨骼（走附属食物贴图）

**除上述三根手臂骨骼外，所有骨骼都会自动走你的食物贴图**。骨骼名可以自由命名（如 `chicken_leg`、`bowl`、`chopsticks`），UV 映射到你的食物贴图(16×16 或 32×32 PNG或64x64可能均可）。

> ⚠️ **常见错误**：把食物骨骼误命名为 `LeftArm`（无空格）或 `left arm`（小写）→ 系统不会识别为手臂骨骼，会按食物贴图渲染，导致手臂显示成食物颜色。
    
###关于模型的UV，这一点非常重要：       （ 这一点非常重要）
   
因为手臂是64×64标准布局，所以整个骨骼模型必须总体UV声明是64×64，不然进入到游戏中，无法渲染完整的皮肤贴图。但是如果我们的食物模型的整体uv不是64×64，你把它放入整体UV是64×64的一个环境中，贴图颜色会错乱。所以，如果已经有模型，你需要重新转换你这个食物物模型UV的大小，在blockbench中新建Bedrock版实体/新建方块中，在非常关键的uv尺寸这里选择64×64，然后导入，最后导出模型整体uv就变成64×64了。


---

## 四、API 参考

### 4.1 `DrinkAnimRegistry.register(...)`

注册一个饮料/食物动画。

```java
public static DrinkAnimHandler register(
        String modId,               // 你的附属 mod id（如 "myaddon"）
        String id,                  // 动画唯一 id（如 "laziji"）
        double duration,            // 动画时长（秒，必须与 animation.json 中的动画时长一致）
        String geoPath,             // geo 文件路径（相对 assets/<modId>/，如 "geo/laziji.geo.json"）
        String animationPath,       // animation 文件路径（如 "animations/laziji.animation.json"）
        String rawAnimationName,    // animation.json 内的动画名（如 "animation.laziji.eat"）
        String texturePath,         // 食物贴图路径（如 "textures/block/laziji.png"）
        String controllerName,      // GeckoLib 控制器名（每个食物必须唯一，如 "laziji_controller"）
        String triggerName,         // 触发器名（通常用 "drink" 或 "eat"）
        List<DrinkSoundCue> soundCues  // 音效时间轴
);
```

**返回值**：`DrinkAnimHandler`，可用于主动触发动画（一般不需要，U 键会自动触发）。

### 4.2 `DrinkAnimRegistry.bindItem(...)`

绑定物品到动画 id，让 U 键自动识别。

```java
public static void bindItem(
        Supplier<Item> itemSupplier,  // 物品供应者（RegistrySupplier 实现了 Supplier）
        String drinkId                // register() 时传入的 id
);
```

### 4.3 `DrinkSoundCue`（音效触发点）

```java
// 单次播放：到点播放一次，播完为止
DrinkSoundCue.once(Supplier<SoundEvent> sound, double timeSeconds);

// 循环播放：到点开始循环，动画结束时自动停止
DrinkSoundCue.loop(Supplier<SoundEvent> sound, double timeSeconds);
```

**时间单位**：秒，动画起点为 0。例如 `DrinkSoundCue.once(SODA_FIZZ, 1.5)` 表示动画开始后 1.5 秒播放一次嘶嘶声。

### 4.4 多贴图支持（可选，v2.1.1+）

如果你的食物模型需要多张贴图（例如辣子鸡的鸡肉、辣椒、盘子分别画在各自贴图上），可以使用 `register` 的重载方法，传入一个 `boneTextures` 骨骼名→贴图路径映射：

```java
public static DrinkAnimHandler register(
        String modId,
        String id,
        double duration,
        String geoPath,
        String animationPath,
        String rawAnimationName,
        String texturePath,           // 默认贴图（boneTextures 未命中的骨骼走这张）
        String controllerName,
        String triggerName,
        List<DrinkSoundCue> soundCues,
        Map<String, String> boneTextures  // 骨骼名 → 贴图路径映射
);
```

**示例**：辣子鸡模型有 3 根食物骨骼 `chicken_meat` / `chili` / `plate`，各自一张贴图：

```java
import java.util.Map;

DrinkAnimRegistry.register(
    "myaddon", "laziji", 6.0,
    "geo/laziji.geo.json",
    "animations/laziji.animation.json",
    "animation.laziji.eat",
    "textures/block/laziji_default.png",   // 默认贴图（fallback）
    "laziji_controller", "eat",
    List.of(
        DrinkSoundCue.once(ModSounds.LAZIJI_SIZZLE, 0.5),
        DrinkSoundCue.once(ModSounds.LAZIJI_EAT, 2.0)
    ),
    Map.of(                                 // 骨骼名 → 贴图路径
        "chicken_meat", "textures/block/laziji_chicken.png",
        "chili",        "textures/block/laziji_chili.png",
        "plate",        "textures/block/laziji_plate.png"
    )
);
```

**贴图查表规则**：

| 骨骼 | 贴图来源 |
|---|---|
| `Left Arm` / `Right Arm` / `Right_Arm2` | 玩家皮肤（自动，无需在 `boneTextures` 里声明） |
| 在 `boneTextures` 中**命中**的骨骼 | 对应的贴图 |
| 其他骨骼（未命中） | `texturePath` 默认贴图 |

**说明**：

- `boneTextures` 的 **key** 是 BlockBench 模型里的骨骼名，**value** 是贴图路径（相对 `assets/<modId>/`，不带 modId 前缀）
- 骨骼名与贴图文件名**没有关系**，可以不同（如骨骼 `chicken_meat` 用贴图 `laziji_chicken.png`）
- 传 `Map.of()`（空 Map）时，所有非手臂骨骼走默认贴图 `texturePath`，等价于 10 参数的旧版 `register`
- 主模组内部共 17 种饮料/零食（矿泉水/奶啤/薯片/能量棒 + 5 种瓶装 + 3 种罐装 + 5 种薯片变体 + 2 种饼干）用的就是 10 参数版本（单贴图），保持向后兼容

> 💡 **何时用多贴图，何时用单贴图**：
> - 模型各部件可以画进**同一张 贴图**（在 BlockBench 里给每根骨骼单独画 UV 区域）→ 用 10 参数 `register`，无需 `boneTextures`
> - 模型部件较多、单张装不下，或想复用其他模型的部件贴图 → 用 11 参数 `register` + `boneTextures`


---

## 五、完整开发流程

下面以"辣子鸡"为例，演示从零开发一个附属食物动画。

### Step 1：BlockBench 建模

1. 新建 BlockBench 模型，选择 **"Java Entity / Player"** 模板（自带玩家皮肤 UV）
2. 保留模板中的 `Left Arm` / `Right Arm` 骨骼（**名字不要改**）
3. 添加 `Right_Arm2` 骨骼（如果需要袖套层）
4. 新建骨骼放辣子鸡模型（骨骼名自由，如 `chicken_plate`、`chili`）
5. 食物骨骼的 UV 映射到你自己的食物贴图

### Step 2：制作动画

1. 在 BlockBench 切换到动画模式，新建动画
2. **动画名**与 `rawAnimationName` 参数一致（如 `animation.laziji.eat`）
3. **动画时长**与 `duration` 参数一致
4. 导出为 `.animation.json`

### Step 3：导出资源

导出 3 个文件放到附属项目的 `src/main/resources/assets/<你的modId>/` 下：

```
assets/myaddon/
├── geo/
│   └── laziji.geo.json              ← BlockBench 导出 Geo 模型
├── animations/
│   └── laziji.animation.json        ← BlockBench 导出动画
└── textures/
    └── block/
        └── laziji.png               ← 食物贴图仅食物骨骼用）
```

> ⚠️ 贴图必须是 **PNG 格式**,手臂骨骼不需要在你的贴图里画。

### Step 4：注册音效

#### 4.1 注册 SoundEvent

```java
// Fabric 1.21.1（Architectury RegistrySupplier 模式）
public static final RegistrySupplier<SoundEvent> LAZIJI_CHILI_SIZZLE = 
    PlatformRegistry.registerSound(
        ResourceLocation.fromNamespaceAndPath(MOD_ID, "laziji.chili_sizzle"),
        () -> SoundEvent.createVariableRangeEvent(
            ResourceLocation.fromNamespaceAndPath(MOD_ID, "laziji.chili_sizzle")));
```

```java
// Forge 1.20.1（DeferredRegister 模式）
public static final DeferredRegister<SoundEvent> SOUNDS = 
    DeferredRegister.create(ForgeRegistries.SOUND_EVENTS, MOD_ID);
public static final RegistryObject<SoundEvent> LAZIJI_CHILI_SIZZLE = 
    SOUNDS.register("laziji_chili_sizzle", 
        () -> SoundEvent.createVariableRangeEvent(
            new ResourceLocation(MOD_ID, "laziji_chili_sizzle")));
```

#### 4.2 写 `sounds.json`

放在 `assets/myaddon/sounds.json`：

```json
{
  "laziji.chili_sizzle": {
    "category": "player",
    "sounds": [
      "myaddon:laziji_chili_sizzle"
    ]
  },
  "laziji.eat": {
    "category": "player",
    "sounds": [
      "myaddon:laziji_eat"
    ]
  }
}
```

> ⚠️ key 必须与 `ModSounds` 里注册的 SoundEvent 的 ResourceLocation path 完全一致。
>
> 💡 `sounds` 数组项既可用裸字符串 `"myaddon:xxx"`（等价于 `{ "name": "myaddon:xxx" }`，默认 `stream: false`），也可用对象形式 `{ "name": "myaddon:xxx", "stream": false }`。短音效用裸字符串更简洁；长音效（如背景音乐、超过 1 分钟的录音）建议用对象形式并显式设 `stream: true` 避免一次性加载到内存。

#### 4.3 放入 .ogg 音频文件

```
assets/myaddon/sounds/
├── laziji_chili_sizzle.ogg
└── laziji_eat.ogg
```

> ⚠️ 音频必须是 **OGG Vorbis** 格式（44100 Hz 单声道，112 kbps）。MP3/WAV/Opus 都不兼容。可用 `ffmpeg -i input.mp3 -ac 1 -ar 44100 -c:a libvorbis -b:a 112k output.ogg` 转换。

### Step 5：注册物品

参考标准 Minecraft 模组物品注册流程。物品必须可食用（`useOn` / `finishUsingItem` 等），但**不需要在物品代码里写动画触发**——主模组的 U 键会自动处理。

### Step 6：调用 API 注册动画

在你的客户端初始化类中（**必须在物品和音效都注册完毕后调用**，建议放在 `ClientSetup` / `clientInit` 阶段）：

> 💡 **下面代码以 Fabric 1.21.1 为例**（import 路径为 `com.realisticdining.fabric.client.arm.drink`）。其他平台的 import 见第十章 10.6 的 **4 平台包路径对照表**，直接替换即可：
> - Forge 1.20.1 → `com.example.realisticdining.forge.client.arm.drink`
> - Fabric 1.20.1 → `com.example.realisticdining.fabric.client.arm.drink`
> - NeoForge 1.21.1 → `com.realisticdining.neoforge.client.arm.drink`

```java
package com.myaddon.client;

import com.realisticdining.fabric.client.arm.drink.DrinkAnimRegistry;
import com.realisticdining.fabric.client.arm.drink.DrinkSoundCue;
import com.myaddon.init.ModItems;
import com.myaddon.init.ModSounds;
import net.minecraft.world.item.Item;

import java.util.List;

public class MyAddonClientSetup {
    
    public static void init() {
        // 1. 注册辣子鸡动画
        DrinkAnimRegistry.register(
            "myaddon",                                    // modId
            "laziji",                                     // 动画 id
            6.0,                                          // 动画时长 6 秒
            "geo/laziji.geo.json",                        // geo 路径
            "animations/laziji.animation.json",           // animation 路径
            "animation.laziji.eat",                       // 动画名
            "textures/block/laziji.png",                  // 贴图路径
            "laziji_controller",                          // 控制器名（必须唯一）
            "eat",                                        // 触发器名
            List.of(                                      // 音效时间轴
                DrinkSoundCue.once(ModSounds.LAZIJI_CHILI_SIZZLE, 0.5),  // 0.5s 嘶嘶声
                DrinkSoundCue.once(ModSounds.LAZIJI_EAT, 2.0),           // 2.0s 吃一口
                DrinkSoundCue.once(ModSounds.LAZIJI_EAT, 3.5),           // 3.5s 再吃一口
                DrinkSoundCue.once(ModSounds.LAZIJI_EAT, 5.0)            // 5.0s 最后吃一口
            )
        );
        
        // 2. 绑定物品 → U 键自动触发
        DrinkAnimRegistry.bindItem(ModItems.LAZIJI, "laziji");
    }
}
```

### Step 6.5：在各平台入口点调用 `MyAddonClientSetup.init()`（**关键**）

上面的 `MyAddonClientSetup.init()` 必须在客户端启动时被调用，否则 API 不会注册。三个平台的调用方式不同：

#### Fabric 1.21.1 / 1.20.1

在 `fabric.mod.json` 中声明 `client` 入口点，对应一个实现 `ClientModInitializer` 的类：

```json
// fabric.mod.json
{
  "entrypoints": {
    "main": ["com.myaddon.MyAddon"],
    "client": ["com.myaddon.client.MyAddonClient"]
  }
}
```

```java
// MyAddonClient.java
package com.myaddon.client;

import net.fabricmc.api.ClientModInitializer;

public class MyAddonClient implements ClientModInitializer {
    @Override
    public void onInitializeClient() {
        // 客户端启动时调用 API 注册
        MyAddonClientSetup.init();
    }
}
```

#### NeoForge 1.21.1

在客户端 mod 事件总线的 `ClientSetup` 阶段调用：

```java
package com.myaddon;

import com.myaddon.client.MyAddonClientSetup;
import net.neoforged.api.distmarker.Dist;
import net.neoforged.bus.api.IEventBus;
import net.neoforged.fml.common.Mod;
import net.neoforged.fml.event.lifecycle.ClientSetupEvent;

@Mod(value = MyAddon.MOD_ID, dist = Dist.CLIENT)
public class MyAddonClient {
    public MyAddonClient(IEventBus modEventBus) {
        modEventBus.addListener(ClientSetupEvent.class, event -> {
            MyAddonClientSetup.init();
        });
    }
}
```

#### Forge 1.20.1

在客户端 mod 事件总线的 `FMLClientSetupEvent` 阶段调用：

```java
package com.myaddon;

import com.myaddon.client.MyAddonClientSetup;
import net.minecraftforge.api.distmarker.Dist;
import net.minecraftforge.eventbus.api.IEventBus;
import net.minecraftforge.fml.common.Mod;
import net.minecraftforge.fml.event.lifecycle.FMLClientSetupEvent;

@Mod(MyAddon.MOD_ID)
public class MyAddon {
    public MyAddon() {
        IEventBus modBus = FMLJavaModLoadingContext.get().getModEventBus();
        modBus.addListener(FMLClientSetupEvent.class, event -> {
            MyAddonClientSetup.init();
        });
    }
}
```

> ⚠️ **常见错误**：
> - 把 `init()` 放在服务端初始化（`onInitialize` / `FMLCommonSetupEvent`）里 → 客户端类加载不到，API 不注册
> - 把 `init()` 放在 `EnqueueIMC` 等过晚阶段 → 物品注册表可能还没就绪
> - 在 Architectury 多平台项目里直接调用 `MyAddonClientSetup.init()` 而不走平台入口 → 服务端启动会崩

### Step 7：构建附属 jar

正常 `./gradlew build` 即可。附属 jar 不需要包含任何渲染代码、Mixin、按键绑定。

---

## 六、完整源码目录结构示例

一个标准的附属项目源码结构（以 **Forge 1.20.1** 为例，其他平台类似，只是入口类和注册 API 不同）：

```
myaddon/
├── src/
│   └── main/
│       ├── java/
│       │   └── com/
│       │       └── myaddon/
│       │           ├── client/
│       │           │   └── MyAddonClientSetup.java      ← 调用 API 的核心类（register + bindItem）
│       │           ├── init/
│       │           │   ├── ModItems.java                ← 物品注册（DeferredRegister + RegistryObject）
│       │           │   └── ModSounds.java               ← 音效事件注册（DeferredRegister + RegistryObject）
│       │           └── MyAddon.java                     ← @Mod 主入口，注册物品/音效 + 监听 FMLClientSetupEvent 调用 init()
│       └── resources/
│           ├── META-INF/
│           │   └── mods.toml                            ← Forge 模组元数据 + 依赖声明（含 realisticdining）
│           ├── assets/
│           │   └── myaddon/
│           │       ├── geo/
│           │       │   └── laziji.geo.json              ← BlockBench 导出的 Geo 模型
│           │       ├── animations/
│           │       │   └── laziji.animation.json        ← BlockBench 导出的动画
│           │       ├── textures/
│           │       │   ├── block/
│           │       │   │   └── laziji.png               ← 食物贴图（PNG，第一人称动画用）
│           │       │   └── item/
│           │       │       └── laziji.png               ← 物品图标（16×16 或 32×32 PNG，GUI 用）
│           │       ├── sounds/
│           │       │   ├── laziji_chili_sizzle.ogg      ← OGG Vorbis 音频（44100 Hz 单声道）
│           │       │   └── laziji_eat.ogg
│           │       ├── sounds.json                      ← 音效事件映射
│           │       ├── models/
│           │       │   └── item/
│           │       │       └── laziji.json              ← 物品模型 JSON
│           │       └── lang/
│           │           ├── en_us.json
│           │           └── zh_cn.json
│           └── data/
│               └── myaddon/
│                   └── recipe/
│                       └── laziji.json                  ← 合成配方（食物附属通常需要）
├── build.gradle                                         ← 声明 Realistic Dining 依赖
└── gradle.properties
```

> 💡 **关于 blockstates/**：第一人称手持食物动画**不需要** blockstates——blockstates 是方块放置用的，附属食物是物品而非方块。除非你的附属同时添加了可放置的方块（如盘子），否则不要建这个目录。

### 关键类说明（Forge 1.20.1）

| 源文件 | 作用 | 何时调用 API |
|---|---|---|
| `MyAddon.java` | `@Mod` 主入口，注册物品/音效到 Forge 注册表，监听 `FMLClientSetupEvent` | 服务端/通用启动阶段注册物品，客户端 setup 阶段触发 `init()` |
| `MyAddonClientSetup.java` | **调用 `DrinkAnimRegistry.register/bindItem` 的核心类** | 由 `MyAddon` 在 `FMLClientSetupEvent` 里调用 |
| `ModItems.java` | 物品 `DeferredRegister` + `RegistryObject` 声明 | 模组加载早期（`@Mod` 构造函数里 `register(modBus)`） |
| `ModSounds.java` | 音效 `DeferredRegister` + `RegistryObject` 声明 | 同上 |

> ⚠️ **关键**：`MyAddonClientSetup.init()` 必须在客户端启动时被调用（见第十章 10.5/10.6 的完整代码）。如果这个方法从未被调用，API 不会注册，U 键不会响应附属物品。
>
> ⚠️ **Forge 1.20.1 的 API 包路径**：`com.example.realisticdining.forge.client.arm.drink.*`（其他平台包名不同，见第十章 10.6 的 4 平台包路径对照表，直接照抄即可）。

---

## 七、调试技巧

### 7.1 动画不播放

1. 检查 `register()` 的 `id` 和 `bindItem()` 的 `drinkId` 是否一致
2. 检查物品是否在手中（U 键检查双手：主手优先，主手无匹配时检查副手）
3. 检查 `register()` 调用时机——必须在客户端初始化阶段，且在物品/音效注册之后
4. 查看日志，搜索 `[DrinkAnimDebug] trigger id=` 看是否触发了动画

### 7.2 模型显示为粉黑方块（贴图丢失）

1. 检查 `texturePath` 是否正确（不带 modId 前缀，主模组会自动加）
2. 检查贴图文件是否在 `assets/<modId>/textures/...` 下
3. 检查贴图是否为 PNG 格式

### 7.3 动画播放但手臂颜色错误

1. 检查手臂骨骼名是否**完全**为 `Left Arm` / `Right Arm` / `Right_Arm2`（注意大小写和空格）
2. 检查手臂骨骼 UV 是否映射到 64×64 玩家皮肤标准布局
3. 不要在你的食物贴图里画手臂——手臂会自动用玩家皮肤

### 7.4 音效不响

1. 检查 `sounds.json` 是否在 `assets/<modId>/` 下且 JSON 格式正确
2. 检查音效文件是否为 OGG Vorbis 格式（不是 Opus）
3. 检查 `DrinkSoundCue` 的时间是否 ≤ `duration`（超出时长的 cue 不会触发）
4. 查看日志，搜索 `[DrinkAnimDebug] playOneShot` 看是否触发

### 7.5 循环音效不停止

如果动画被打断（如切换物品），循环音效会继续播放直到动画自然结束的时长。这是已知行为。如果你需要循环音效随物品切换立即停止，目前 API 不支持——请使用单次音效。

---

## 八、限制与注意事项

| 限制 | 说明 |
|---|---|
| 动画时长 | 无硬性上限（`double` 秒），但建议控制在 1~30 秒，过长会让玩家等待 |
| 音效格式 | 必须 OGG Vorbis，44100 Hz 单声道 |
| 贴图格式 | 必须 PNG，建议64×64 |
| 骨骼命名 | 手臂骨骼名严格区分大小写和空格 |
| 控制器名 | 每个动画必须唯一（同附属内不可重复，也不能与主模组冲突） |
| U 键响应 | 双手都检查，主手优先；主手无匹配时检查副手（v2.1.1+） |
| 服务端 | 动画纯客户端，服务端无感知 |

---

## 九、License 与署名

### 你可以做的

- ✅ 自由开发并发布 Realistic Dining 的附属模组
- ✅ 附属可以开源或闭源，可选择任意 License
- ✅ 在附属描述中提及"基于 Realistic Dining 开发"（建议但非强制）
- ✅ 商业化（如 Patreon/CurseForge 积分下载）

### 你不可以做的

- ❌ 复制 Realistic Dining 的核心渲染代码到独立模组（非附属形式）
- ❌ 绕过 API
- ❌ 移除 Realistic Dining 的依赖声明、将其包装成独立模组发布
- ❌ 声称 Realistic Dining 的动画系统为你原创

### 建议署名

附属发布时建议在描述中加入：

```
本模组是 Realistic Dining 的附属，需要先安装 Realistic Dining 才能使用。
动画系统由 Realistic Dining 提供。
```

---

## 十、附录：Forge 1.20.1 完整端到端示例

> 本章以 1.20.1 Forge 为例，从空项目到可发布的附属 jar，列出**所有需要写的代码和配置**。其他平台结构类似，只是入口点和注册 API 不同。

### 10.1 build.gradle：声明 Realistic Dining 依赖

附属必须把 Realistic Dining 加到编译路径，否则 `import com.realisticdining.*` 编译不过。

```gradle
// build.gradle
repositories {
    maven {
        name = "CurseMaven"
        url = "https://cursemaven.com"
        // 如果 Realistic Dining 发布在 CurseForge，用 CurseMaven 拉取
    }
    maven {
        name = "ModrinthMaven"
        url = "https://api.modrinth.com/maven"
        // 如果发布在 Modrinth，用 ModrinthMaven 拉取
    }
}

dependencies {
    minecraft "net.minecraftforge:forge:1.20.1-47.3.0"
    
    // 以 Realistic Dining 2.0.4 为例（具体坐标以官方发布页为准）：
    implementation fg.deobf("curse.maven:realisticdining-<projectId>:<fileId>")
    // 或
    implementation fg.deobf("maven.modrinth:realisticdining:2.0.4+forge-1.20.1")
}
```

> ⚠️ **坐标确认**：具体 CurseForge projectId / Modrinth version 号请到 Realistic Dining 官方发布页查看。如果你拿到的是本地 jar，可以 `flatDir` 引用：
> ```gradle
> repositories { flatDir { dirs 'libs' } }
> dependencies { implementation fg.deobf(name: "realisticdining-forge-2.0.4") }
> ```

> ⚠️ **严禁自写 API 存根类（stub）绕过依赖**：
>
> 如果暂时拿不到 Realistic Dining jar，**不要**为了"让编译通过"自己手写一个签名相似的空实现类（例如 `DrinkAnimRegistry` / `DrinkSoundCue` 的 stub）放到 `src/main/java` 下。
>
> 原因：**Java 中方法的返回类型属于方法签名的一部分**。即使你把方法名、参数列表都写对了，只要返回类型与真实 API 不一致（例如存根写成返回 `Object`，真实 API 返回 `DrinkAnimHandler`），编译能通过，但运行时 JVM 在 linkage 阶段会找不到匹配的方法，抛出 `NoSuchMethodError` 导致游戏崩溃。
>
> 正确做法：把玩家游戏目录 `mods/` 下的 `realisticdining-forge-<version>.jar` 复制到项目的 `libs/` 目录，用 `compileOnly` 引用（编译期可见、不打包进附属 jar，运行时由主模组提供）：
>
> ```gradle
> dependencies {
>     minecraft "net.minecraftforge:forge:1.20.1-47.3.0"
>     compileOnly files("libs/realisticdining-forge-2.0.4.jar")
> }
> ```
>
> 这样 `javac` 会直接用真实 jar 校验方法签名，存根陷阱从根源上消除。

### 10.2 mods.toml：模组元数据 + 依赖声明

文件路径：`src/main/resources/META-INF/mods.toml`

```toml
modLoader = "javafml"
loaderVersion = "[47,)"
license = "你的 License"

[[mods]]
modId = "myaddon"
version = "1.0.0"
displayName = "My Addon: Laziji"
authors = "你的名字"
description = '''
Realistic Dining 的附属，添加辣子鸡食用动画。
'''

# 依赖 Minecraft
[[dependencies.myaddon]]
modId = "minecraft"
mandatory = true
versionRange = "[1.20.1,1.21)"
ordering = "NONE"
side = "BOTH"

# 依赖 Forge
[[dependencies.myaddon]]
modId = "forge"
mandatory = true
versionRange = "[47,)"
ordering = "NONE"
side = "BOTH"

# 依赖 Realistic Dining（关键！）
[[dependencies.myaddon]]
modId = "realisticdining"
mandatory = true
versionRange = "[2.0.4,)"
ordering = "AFTER"
side = "BOTH"
```

> ⚠️ `ordering = "AFTER"` 确保 Realistic Dining 先于你的附属加载，否则 API 类还没就绪时调用 `register` 会崩。

### 10.3 ModItems.java：物品注册

文件路径：`src/main/java/com/myaddon/init/ModItems.java`

```java
package com.myaddon.init;

import com.myaddon.MyAddon;
import net.minecraft.world.food.FoodProperties;
import net.minecraft.world.item.Item;
import net.minecraftforge.eventbus.api.IEventBus;
import net.minecraftforge.registries.DeferredRegister;
import net.minecraftforge.registries.ForgeRegistries;
import net.minecraftforge.registries.RegistryObject;

public class ModItems {
    // 1. 创建 DeferredRegister
    public static final DeferredRegister<Item> ITEMS =
            DeferredRegister.create(ForgeRegistries.ITEMS, MyAddon.MOD_ID);
    
    // 2. 注册辣子鸡物品（可食用，8 饱食度）
    public static final RegistryObject<Item> LAZIJI = ITEMS.register("laziji",
            () -> new Item(new Item.Properties()
                    .food(new FoodProperties.Builder()
                            .nutrition(8)
                            .saturationMod(0.8F)
                            .build())
                    .stacksTo(16)));
    
    // 3. 在主类里调用 ITEMS.register(modEventBus)
    public static void register(IEventBus modEventBus) {
        ITEMS.register(modEventBus);
    }
}
```

### 10.4 ModSounds.java：音效注册

文件路径：`src/main/java/com/myaddon/init/ModSounds.java`

```java
package com.myaddon.init;

import com.myaddon.MyAddon;
import net.minecraft.resources.ResourceLocation;
import net.minecraft.sounds.SoundEvent;
import net.minecraftforge.eventbus.api.IEventBus;
import net.minecraftforge.registries.DeferredRegister;
import net.minecraftforge.registries.ForgeRegistries;
import net.minecraftforge.registries.RegistryObject;

import java.util.function.Supplier;

public class ModSounds {
    public static final DeferredRegister<SoundEvent> SOUNDS =
            DeferredRegister.create(ForgeRegistries.SOUND_EVENTS, MyAddon.MOD_ID);
    
    // 3 个音效事件（与 sounds.json 里的 key 对应）
    public static final RegistryObject<SoundEvent> LAZIJI_CHILI_SIZZLE =
            SOUNDS.register("laziji_chili_sizzle", () ->
                    SoundEvent.createVariableRangeEvent(
                            new ResourceLocation(MyAddon.MOD_ID, "laziji_chili_sizzle")));
    
    public static final RegistryObject<SoundEvent> LAZIJI_EAT =
            SOUNDS.register("laziji_eat", () ->
                    SoundEvent.createVariableRangeEvent(
                            new ResourceLocation(MyAddon.MOD_ID, "laziji_eat")));
    
    // RegistryObject 实现了 Supplier<SoundEvent>，可直接传给 DrinkSoundCue
    public static Supplier<SoundEvent> chiliSizzle() { return LAZIJI_CHILI_SIZZLE; }
    public static Supplier<SoundEvent> eat() { return LAZIJI_EAT; }
    
    public static void register(IEventBus modEventBus) {
        SOUNDS.register(modEventBus);
    }
}
```

### 10.5 MyAddon.java：主入口

文件路径：`src/main/java/com/myaddon/MyAddon.java`

```java
package com.myaddon;

import com.myaddon.init.ModItems;
import com.myaddon.init.ModSounds;
import net.minecraft.world.item.CreativeModeTabs;
import net.minecraftforge.common.MinecraftForge;
import net.minecraftforge.event.BuildCreativeModeTabContentsEvent;
import net.minecraftforge.eventbus.api.IEventBus;
import net.minecraftforge.fml.common.Mod;
import net.minecraftforge.fml.event.lifecycle.FMLClientSetupEvent;
import net.minecraftforge.fml.javafmlmod.FMLJavaModLoadingContext;

@Mod(MyAddon.MOD_ID)
public class MyAddon {
    public static final String MOD_ID = "myaddon";
    
    public MyAddon() {
        IEventBus modBus = FMLJavaModLoadingContext.get().getModEventBus();
        
        // 1. 注册物品和音效（必须在客户端 setup 之前）
        ModItems.register(modBus);
        ModSounds.register(modBus);
        
        // 2. 添加到创造模式物品栏
        modBus.addListener(this::addCreative);
        
        // 3. 客户端启动时调用 API 注册（关键！）
        modBus.addListener(FMLClientSetupEvent.class, event -> {
            com.myaddon.client.MyAddonClientSetup.init();
        });
        
        MinecraftForge.EVENT_BUS.register(this);
    }
    
    private void addCreative(BuildCreativeModeTabContentsEvent event) {
        if (event.getTabKey() == CreativeModeTabs.FOOD_AND_DRINKS) {
            event.accept(ModItems.LAZIJI);
        }
    }
}
```

### 10.6 MyAddonClientSetup.java：调用 API 的核心类

文件路径：`src/main/java/com/myaddon/client/MyAddonClientSetup.java`

```java
package com.myaddon.client;

import com.myaddon.init.ModItems;
import com.myaddon.init.ModSounds;
import com.example.realisticdining.forge.client.arm.drink.DrinkAnimRegistry;
import com.example.realisticdining.forge.client.arm.drink.DrinkSoundCue;

import java.util.List;

public class MyAddonClientSetup {
    
    public static void init() {
        // === 调用 公共 API ===
       
        DrinkAnimRegistry.register(
            "myaddon",                                    // modId（必须与 mods.toml 一致）
            "laziji",                                     // 动画 id
            6.0,                                          // 动画时长 6 秒
            "geo/laziji.geo.json",                        // geo 文件路径
            "animations/laziji.animation.json",           // animation 文件路径
            "animation.laziji.eat",                       // animation.json 内的动画名
            "textures/block/laziji.png",                  // 食物贴图路径
            "laziji_controller",                          // GeckoLib 控制器名（必须唯一）
            "eat",                                        // 触发器名
            List.of(                                      // 音效时间轴
                DrinkSoundCue.once(ModSounds.LAZIJI_CHILI_SIZZLE, 0.5),  // 0.5s 嘶嘶声
                DrinkSoundCue.once(ModSounds.LAZIJI_EAT, 2.0),           // 2.0s 吃一口
                DrinkSoundCue.once(ModSounds.LAZIJI_EAT, 3.5),           // 3.5s 再吃一口
                DrinkSoundCue.once(ModSounds.LAZIJI_EAT, 5.0)            // 5.0s 最后吃一口
            )
        );
        
        // 2. 绑定物品 → U 键自动触发
        DrinkAnimRegistry.bindItem(ModItems.LAZIJI, "laziji");
    }
}
```

> ⚠️ **import 路径按平台对照下表**：
>
> | 平台 | API 包路径 |
> |---|---|
> | Forge 1.20.1 | `com.example.realisticdining.forge.client.arm.drink` |
> | Fabric 1.20.1 | `com.example.realisticdining.fabric.client.arm.drink` |
> | Fabric 1.21.1 | `com.realisticdining.fabric.client.arm.drink` |
> | NeoForge 1.21.1 | `com.realisticdining.neoforge.client.arm.drink` |
>
> 例如 Forge 1.20.1 附属的 import：
> ```java
> import com.example.realisticdining.forge.client.arm.drink.DrinkAnimRegistry;
> import com.example.realisticdining.forge.client.arm.drink.DrinkSoundCue;
> ```
>
> 例如 Fabric 1.21.1 附属的 import：
> ```java
> import com.realisticdining.fabric.client.arm.drink.DrinkAnimRegistry;
> import com.realisticdining.fabric.client.arm.drink.DrinkSoundCue;
> ```
>
> 💡 **注意**：1.20.1 两个平台的包名带 `example`（历史遗留），1.21.1 两个平台不带 `example`。请严格按上表 import，不要混用。

### 10.7 sounds.json：音效事件映射

文件路径：`src/main/resources/assets/myaddon/sounds.json`

```json
{
  "laziji.chili_sizzle": {
    "category": "player",
    "sounds": [
      "myaddon:laziji_chili_sizzle"
    ]
  },
  "laziji.eat": {
    "category": "player",
    "sounds": [
      "myaddon:laziji_eat"
    ]
  }
}
```

> ⚠️ key 必须与 `ModSounds` 里注册的 SoundEvent 的 ResourceLocation path 完全一致（dotted 或 underscored 风格均可，但两边必须匹配）。主模组自身采用 dotted 风格（如 `drink.crisp.bag_pickup`），建议附属也用 dotted 保持风格统一。
>
> 💡 `sounds` 数组项既可用裸字符串 `"myaddon:xxx"`（等价于 `{ "name": "myaddon:xxx" }`，默认 `stream: false`），也可用对象形式 `{ "name": "myaddon:xxx", "stream": false }`。短音效用裸字符串更简洁；长音效（如背景音乐、超过 1 分钟的录音）建议用对象形式并显式设 `stream: true` 避免一次性加载到内存。

### 10.8 物品模型 JSON

文件路径：`src/main/resources/assets/myaddon/models/item/laziji.json`

```json
{
  "parent": "minecraft:item/generated",
  "textures": {
    "layer0": "myaddon:item/laziji"
  }
}
```

> 对应的物品图标贴图放在 `assets/myaddon/textures/item/laziji.png`（这是 GUI 里显示的图标，与第一人称动画用的 `textures/block/laziji.png` 是两张不同的图）。

### 10.9 语言文件

文件路径：`src/main/resources/assets/myaddon/lang/zh_cn.json`

```json
{
  "item.myaddon.laziji": "辣子鸡",
  "item.myaddon.laziji.tooltip": "按 U 键食用（手中持有即可）"
}
```

文件路径：`src/main/resources/assets/myaddon/lang/en_us.json`

```json
{
  "item.myaddon.laziji": "Laziji",
  "item.myaddon.laziji.tooltip": "Press U to eat (hold in either hand)"
}
```

### 10.10 资源文件清单

以下是必须准备的资源文件，全部放在 `src/main/resources/assets/myaddon/` 下：

| 文件 | 来源 | 格式要求 |
|---|---|---|
| `geo/laziji.geo.json` | BlockBench 导出 | Geo JSON，骨骼含 `Left Arm` / `Right Arm` / `Right_Arm2` |
| `animations/laziji.animation.json` | BlockBench 导出 | 动画名 = `animation.laziji.eat`，时长 = 6.0 秒 |
| `textures/block/laziji.png` | 自绘 | PNG(16×16 或 32×32 PNG或64x64可能均可），食物贴图（手臂骨骼不画） |
| `textures/item/laziji.png` | 自绘 | PNG，16×16 或 32×32，GUI 图标 |
| `sounds/laziji_chili_sizzle.ogg` | 音频转换 | OGG Vorbis，44100 Hz 单声道 |
| `sounds/laziji_eat.ogg` | 音频转换 | OGG Vorbis，44100 Hz 单声道 |
| `sounds.json` | 手写 | 见 10.7 |
| `models/item/laziji.json` | 手写 | 见 10.8 |
| `lang/zh_cn.json` / `en_us.json` | 手写 | 见 10.9 |

### 10.11 验证清单

完成开发后，逐项核对：

- [ ] `build.gradle` 已添加 Realistic Dining 依赖，`./gradlew build` 编译通过
- [ ] `mods.toml` 的 `realisticdining` 依赖 `versionRange = "[2.0.4,)"` 且 `ordering = "AFTER"`
- [ ] `MyAddon.java` 在 `FMLClientSetupEvent` 里调用了 `MyAddonClientSetup.init()`
- [ ] `MyAddonClientSetup.init()` 里调用了 `DrinkAnimRegistry.register(...)` 和 `bindItem(...)`
- [ ] geo 文件含 `Left Arm` / `Right Arm` / `Right_Arm2` 三根骨骼（大小写、空格完全一致）
- [ ] animation 文件里的动画名与 `register` 的 `rawAnimationName` 参数一致
- [ ] 动画时长与 `register` 的 `duration` 参数一致
- [ ] 控制器名 `laziji_controller` 全模组唯一（不与主模组或其他附属冲突）
- [ ] 所有音效为 OGG Vorbis 格式（不是 MP3/WAV/Opus）
- [ ] `sounds.json` 的 key 与 `ModSounds` 注册名一致
- [ ] 物品贴图（GUI 用）和食物贴图（动画用）是两张不同的图，路径不混淆
- [ ] 启动游戏后，主手或副手拿辣子鸡按 U 键能看到动画并听到音效
- [ ] 切换到其他物品时动画停止
- [ ] 玩家换皮肤后手臂贴图自动更新（无需重启游戏）

### 10.12 常见编译/运行错误

| 错误 | 原因 | 解决 |
|---|---|---|
| `com.realisticdining.* cannot be resolved` | build.gradle 没加 Realistic Dining 依赖 | 见 10.1 |
| `ClassCastException: Item cannot be cast to Supplier<Item>` | `bindItem` 第一个参数传了 Item 而非 RegistryObject | `bindItem(ModItems.LAZIJI, ...)` 而非 `bindItem(ModItems.LAZIJI.get(), ...)` |
| 启动崩溃 `NoSuchMethodError: register` | Realistic Dining 版本低于 2.0.4 | 升级到 2.0.4+ |
| 启动崩溃 `NoSuchMethodError` 且返回类型不匹配（如报 `Object ...register(...)`） | 自写了 API 存根类（stub）绕过 jar 依赖，存根方法签名与真实 API 不一致（返回类型也是签名的一部分） | 删除存根类，改用真实 jar 作 `compileOnly` 依赖编译，见 10.1 |
| `RealisticDining` 加载顺序在 `myaddon` 之后 | mods.toml 没写 `ordering = "AFTER"` | 见 10.2 |
| U 键无反应 | `MyAddonClientSetup.init()` 没被调用 | 检查 `@Mod` 类是否注册了 `FMLClientSetupEvent` 监听器 |
| 动画播放但手臂颜色错误 | 骨骼名不是 `Left Arm`（缺空格或大小写错） | 在 BlockBench 里改名 |
| 动画播放但食物是粉黑方块 | 贴图路径错误或贴图不是 PNG | 检查 `texturePath` 和文件格式 |

---

## 十一、双模型 API（v2.0.7+）

### 11.1 什么是双模型 API

普通单模型 API（第四章 4.1/4.4）注册一个 geo 模型，模型内同时包含 `Left Arm` / `Right Arm` / `Right_Arm2` 三根手臂骨骼 + 食物骨骼，Renderer 按骨骼名切贴图实现"手臂用皮肤、食物用饮料贴图"的视觉合成。

**双模型 API** 注册**两个独立 geo 模型**，每帧同帧依次渲染两个模型，类似吃米饭动画的"左臂+米饭碗"和"右臂+筷子"双模型架构。适用于适用于左手模形动画保留最后一帧，右手模形动画只播放一次，或者反过来，本模组的米饭，就是用这样子的双模型。
### 11.2 API 签名

```java
public static DrinkAnimHandler register(
        String modId,                 // 附属 mod id
        String id,                    // 动画唯一 id
        double duration,              // 动画时长（秒，两组必须一致）
        // 第一组（如左臂+物品）
        String geoPath1,              // 第一组 geo 路径
        String animationPath1,        // 第一组 animation 路径
        String rawAnimationName1,     // 第一组 animation.json 内的动画名
        String texturePath1,          // 第一组默认贴图
        String controllerName1,       // 第一组 GeckoLib controller 名（必须唯一）
        String triggerName1,          // 第一组 trigger 名
        // 第二组（如右臂+物品）
        String geoPath2,              // 第二组 geo 路径
        String animationPath2,        // 第二组 animation 路径
        String rawAnimationName2,     // 第二组 animation.json 内的动画名
        String texturePath2,          // 第二组默认贴图
        String controllerName2,       // 第二组 GeckoLib controller 名（必须唯一，且 ≠ controllerName1）
        String triggerName2,          // 第二组 trigger 名
        // 共享
        List<DrinkSoundCue> soundCues,  // 音效时间轴（两组共享，只播一遍）
        Map<String, String> boneTextures1,  // 第一组骨骼名 → 贴图路径映射；可为空 Map
        Map<String, String> boneTextures2   // 第二组骨骼名 → 贴图路径映射；可为空 Map
);
```

> ⚠️ **参数顺序与单模型 API 不同，别写错！**
>
> 单模型 API（4.1/4.4 节）的参数顺序是：`...triggerName, soundCues, boneTextures`
>
> 双模型 API 的参数顺序是：`...triggerName1, [6 个第二组参数], soundCues, boneTextures1, boneTextures2`
>
> **音效（soundCues）和贴图映射（boneTextures1/2）被推到了参数列表最后**，因为它们是两组共享的。从单模型升级到双模型时，原 `soundCues` 位置必须改成第二组参数，否则编译报错"List 无法转换为 String"。
>
> 共 **18 个参数**：3（modId/id/duration）+ 6（第一组）+ 6（第二组）+ 3（共享）= 18。

> 💡 **import 路径按平台对照下表**（与第十章 10.6 节一致）：
>
> | 平台 | API 包路径 |
> |---|---|
> | Forge 1.20.1 | `com.example.realisticdining.forge.client.arm.drink` |
> | Fabric 1.20.1 | `com.example.realisticdining.fabric.client.arm.drink` |
> | Fabric 1.21.1 | `com.realisticdining.fabric.client.arm.drink` |
> | NeoForge 1.21.1 | `com.realisticdining.neoforge.client.arm.drink` |
>
> 1.20.1 两个平台的包名带 `example`（历史遗留），1.21.1 两个平台不带 `example`。下方 11.4 示例以 Fabric 1.21.1 为例，其他平台请替换 import。

### 11.3 双模型设计约定

#### 骨骼命名

每个 geo 模型内部仍遵循第三章的手臂骨骼命名约定：
- **第一组模型**（通常画左臂 + 物品）：含 `Left Arm` 骨骼（UV 映射到玩家皮肤 64×64 左臂区）+ 食物骨骼（UV 映射到第一组贴图）
- **第二组模型**（通常画右臂 + 物品）：含 `Right Arm` / `Right_Arm2` 骨骼（UV 映射到玩家皮肤 64×64 右臂区）+ 食物骨骼（UV 映射到第二组贴图）

> ⚠️ **不要在同一组模型里同时画左右两只手臂骨骼**——这样会导致两组模型都画了同一只手，渲染重叠。每组模型只画自己负责的那只手。

#### 贴图

- 第一组贴图（`texturePath1` / `boneTextures1`）：只装第一组模型的食物骨骼贴图， PNG
- 第二组贴图（`texturePath2` / `boneTextures2`）：只装第二组模型的食物骨骼贴图，PNG
- 手臂骨骼不画贴图（自动用玩家皮肤）

#### 控制器名

`controllerName1` 和 `controllerName2` 必须互不相同，且与模组内其他动画的 controller 名不冲突。

#### 动画时长

`duration` 对两组都生效，两组的 animation.json 内动画实际时长必须与 `duration` 一致。

### 11.4 完整示例：双手喝汤（左拿汤勺右拿碗）

> 💡 **下方代码以 Fabric 1.21.1 为例**（import 为 `com.realisticdining.fabric.client.arm.drink.*`）。其他平台请按 11.2 节末尾的包路径表替换 import：
> - Forge 1.20.1 → `com.example.realisticdining.forge.client.arm.drink.*`
> - Fabric 1.20.1 → `com.example.realisticdining.fabric.client.arm.drink.*`
> - NeoForge 1.21.1 → `com.realisticdining.neoforge.client.arm.drink.*`

```java
package com.myaddon.client;

import com.realisticdining.fabric.client.arm.drink.DrinkAnimRegistry;
import com.realisticdining.fabric.client.arm.drink.DrinkSoundCue;
import com.myaddon.init.ModItems;
import com.myaddon.init.ModSounds;
import net.minecraft.world.item.Item;

import java.util.List;
import java.util.Map;

public class MyAddonClientSetup {

    public static void init() {
        // 双模型注册：左拿汤勺（第一组）+ 右拿碗（第二组）
        DrinkAnimRegistry.register(
            "myaddon",                                      // modId
            "soup_bowl",                                    // 动画 id
            6.0,                                            // 动画时长 6 秒（两组一致）
            // === 第一组：左臂 + 汤勺 ===
            "geo/soup_spoon.geo.json",                      // geo1：左臂+汤勺模型
            "animations/soup_spoon.animation.json",         // animation1
            "animation.soup_spoon.scoop",                   // rawAnimationName1
            "textures/block/soup_spoon.png",                // texture1：汤勺贴图
            "soup_spoon_controller",                        // controller1（唯一）
            "scoop",                                        // trigger1
            // === 第二组：右臂 + 碗 ===
            "geo/soup_bowl.geo.json",                       // geo2：右臂+碗模型
            "animations/soup_bowl.animation.json",          // animation2
            "animation.soup_bowl.hold",                     // rawAnimationName2
            "textures/block/soup_bowl.png",                 // texture2：碗贴图
            "soup_bowl_controller",                         // controller2（唯一，≠ controller1）
            "hold",                                         // trigger2
            // === 共享音效 ===
            List.of(
                DrinkSoundCue.once(ModSounds.SOUP_SCOOP, 0.5),
                DrinkSoundCue.once(ModSounds.SOUP_SLURP, 2.5),
                DrinkSoundCue.once(ModSounds.SOUP_SLURP, 4.0),
                DrinkSoundCue.once(ModSounds.SOUP_AHH, 5.5)
            ),
            // === 骨骼贴图映射（无自定义可传 Map.of()） ===
            Map.of(),                                       // boneTextures1
            Map.of()                                        // boneTextures2
        );

        // 绑定物品 → U 键自动触发
        DrinkAnimRegistry.bindItem(ModItems.SOUP_BOWL, "soup_bowl");
    }
}
```

### 11.5 双模型资源文件清单

以 11.4 的汤碗为例，附属资源结构：

```
assets/myaddon/
├── geo/
│   ├── soup_spoon.geo.json        ← 第一组：左臂+汤勺
│   └── soup_bowl.geo.json         ← 第二组：右臂+碗
├── animations/
│   ├── soup_spoon.animation.json  ← 第一组动画
│   └── soup_bowl.animation.json   ← 第二组动画
├── textures/
│   └── block/
│       ├── soup_spoon.png         ← 第一组贴图
│       └── soup_bowl.png          ← 第二组贴图
├── sounds/
│   ├── soup_scoop.ogg
│   ├── soup_slurp.ogg
│   └── soup_ahh.ogg
├── sounds.json
├── models/item/soup_bowl.json
└── lang/...
```

### 11.6 双模型调试技巧

1. **两组动画不同步**：检查两组 animation.json 的动画时长是否一致，是否都与 `duration` 参数一致
2. **只有一组动画播放**：检查 `controllerName1` / `controllerName2` 是否互不相同；查看日志搜索 `[DrinkAnimDebug] trigger` 看 `dual=true` 是否打印
3. **手臂贴图错乱**：检查第一组模型是否只含 `Left Arm`、第二组是否只含 `Right Arm` / `Right_Arm2`，不要两组都画同一只手
4. **贴图串味**：第一组食物骨骼走 `texturePath1` / `boneTextures1`，第二组走 `texturePath2` / `boneTextures2`，确认两组贴图路径不混淆


---

## 十二、U 键双手检查（v2.1.1+）

### 12.1 行为说明

从 v2.1.1 起，U 键触发动画时检查双手：

1. **主手优先**：先检查主手物品是否绑定了动画
2. **主手无匹配时检查副手**：主手为空、或主手物品未绑定动画时，检查副手物品
3. **双手都无匹配**：无反应

### 12.2 优先级示例

| 主手 | 副手 | 按 U 触发的动画 |
|---|---|---|
| 矿泉水（绑 mineral_water） | 任意 | mineral_water（主手优先） |
| 空手 | 牛奶（绑 milk） | milk（副手） |
| 镐子（未绑定） | 能量棒（绑 energy_bar） | energy_bar（副手） |
| 矿泉水（绑 mineral_water） | 牛奶（绑 milk） | mineral_water（主手优先，副手被忽略） |
| 镐子（未绑定） | 镐子（未绑定） | 无反应 |




---

## 十三、Pickup/Putdown 动画（v2.1.3+，方案 B）

### 13.1 行为说明：五阶段状态机

从 v2.1.3 起，饮料/零食动画支持「拿起 → 持物 → 食用 → 放下」五阶段状态机（方案 B）：

```
IDLE ──拿到物品──▶ PICKUP ──播完──▶ HOLD ──按U键──▶ DRINK ──播完──▶ IDLE
                                  └──切物品──▶ PUTDOWN ──播完──▶ IDLE
IDLE ──按U键(无pickup配置)──▶ DRINK ──播完──▶ IDLE   （旧饮料兼容路径）
```

| 阶段 | 触发方式 | 行为 | 音效 |
|---|---|---|---|
| `IDLE` | 初始态 | 无渲染 | 无 |
| `PICKUP` | 物品进入主手自动触发 | 播放"拿起"骨骼动画 | 无（pickup 阶段不推进音效 cue） |
| `HOLD` | PICKUP 播完自动进入 | **定格在 PICKUP 最后一帧**（持物等待），屏蔽原版双手 | 无 |
| `DRINK` | HOLD 态按 U 键触发 | 播放"食用"动画，触发音效 cue 与消耗包 | 有 |
| `PUTDOWN` | 主手切出物品自动触发 | 播放"放下"反向动画 | 无 |

**关键设计**：

- **PICKUP 播完后定格最后一帧**：使用 GeckoLib 的 `RawAnimation.thenPlayAndHold`，HOLD 态无需额外循环动画，GeckoLib 自动保持骨骼姿态
- **PICKUP 中按 U 键排队**：状态机内部 `pendingDrink` 标志，PICKUP → HOLD 转换时自动触发 DRINK，不会丢失按键输入
- **切物品时播"放下"反向动画**：FpArmRenderSystem 每帧检测主手物品变化，旧物品在 HOLD 态时触发 PUTDOWN；新物品进入主手时触发 PICKUP
- **PUTDOWN 进行中切到新物品**：新物品的 PICKUP 排队（`pendingPickupItem`），等所有动画结束（全 IDLE）后才触发，避免动画重叠
- **未配置 putdown 动画**：切物品时直接 `reset()`，物品瞬间消失（不播下动画）

### 13.2 API 签名

```java
public static void registerPickup(
        DrinkAnimHandler handler,           // register() 返回的 handler
        String pickupRawAnimName,           // pickup 动画名（animation.json 内，如 "animation.bottle.pickup"）
        double pickupDuration,              // pickup 时长（秒，应与 animation.json 内动画长度一致）
        String putdownRawAnimName,          // putdown 动画名（可为 null 表示不播放下动画，切物品时瞬间消失）
        double putdownDuration              // putdown 时长（秒，putdownRawAnimName 为 null 时可传 0）
);
```

> ⚠️ **必须先调用 `register` 拿到 handler，再调用 `registerPickup`**。`registerPickup` 是在已有 handler 上追加配置，不创建新动画。
>
> ⚠️ **未调用 `registerPickup` 的饮料走旧路径**：IDLE ↔ DRINK（无 PICKUP/HOLD/PUTDOWN），保持向后兼容。主模组内置 17 种饮料/零食均未配置 pickup，行为与 v2.0.x 一致。

### 13.3 动画文件要求

**pickup / putdown / drink 三个动画必须能在同一份 animation.json 中被找到**（GeckoLib GeoModel 架构限制：单个 GeoModel 只持有一个 animationResource，drink/pickup/putdown 三个 trigger 共用同一 controller，因此三个动画名必须都能从该 Model 的 `getAnimationResource()` 返回的同一份 JSON 中查到）。三个动画共用同一份 geo 模型。

> ⚠️ 本限制由 GeckoLib 架构隐性保证，无运行时校验。若 pickup/putdown 动画名在 animation.json 中缺失，GeckoLib 会在触发时抛出"animation not found"异常并打印警告日志，但不会导致游戏崩溃。

animation.json 示例结构：

```json
{
  "format_version": "1.8.0",
  "animations": {
    "animation.bottle.pickup":   { "animation_length": 0.5, "bones": { ... } },
    "animation.bottle.drink":    { "animation_length": 6.0, "bones": { ... } },
    "animation.bottle.putdown":  { "animation_length": 0.4, "bones": { ... } }
  }
}
```

**pickup 动画最后一帧的骨骼姿态 = HOLD 态持物姿态**。GeckoLib 用 `thenPlayAndHold` 自动定格，无需在 BlockBench 里设置循环点。

**putdown 动画建议是 pickup 的反向播放**（在 BlockBench 里把 pickup 关键帧倒序排列），让"拿起"和"放下"视觉对称。

### 13.4 完整示例：带 pickup/putdown 的辣子鸡

> 💡 下方代码以 Fabric 1.21.1 为例（import 为 `com.realisticdining.fabric.client.arm.drink.*`）。其他平台请按 10.6 节的包路径表替换 import。

```java
package com.myaddon.client;

import com.realisticdining.fabric.client.arm.drink.DrinkAnimHandler;
import com.realisticdining.fabric.client.arm.drink.DrinkAnimRegistry;
import com.realisticdining.fabric.client.arm.drink.DrinkSoundCue;
import com.myaddon.init.ModItems;
import com.myaddon.init.ModSounds;

import java.util.List;

public class MyAddonClientSetup {

    public static void init() {
        // 1. 注册辣子鸡食用动画（与第十章 10.6 一致）
        DrinkAnimHandler handler = DrinkAnimRegistry.register(
            "myaddon", "laziji", 6.0,
            "geo/laziji.geo.json",
            "animations/laziji.animation.json",
            "animation.laziji.eat",
            "textures/block/laziji.png",
            "laziji_controller", "eat",
            List.of(
                DrinkSoundCue.once(ModSounds.LAZIJI_CHILI_SIZZLE, 0.5),
                DrinkSoundCue.once(ModSounds.LAZIJI_EAT, 2.0),
                DrinkSoundCue.once(ModSounds.LAZIJI_EAT, 3.5),
                DrinkSoundCue.once(ModSounds.LAZIJI_EAT, 5.0)
            )
        );

        // 2. 追加 pickup/putdown 动画配置（v2.1.3+）
        DrinkAnimRegistry.registerPickup(
            handler,
            "animation.laziji.pickup",   0.5,    // pickup 0.5 秒
            "animation.laziji.putdown",  0.4     // putdown 0.4 秒（传 null 则切物品时瞬间消失）
        );

        // 3. 绑定物品 → U 键自动触发
        DrinkAnimRegistry.bindItem(ModItems.LAZIJI, "laziji");
    }
}
```

> ⚠️ **双模型动画也可以配置 pickup/putdown**：`registerPickup` 内部会同时为两组 animatable 注册 pickup/putdown trigger，两组共用同一个 pickup/putdown 动画名（要求两组的 animation.json 内都有同名 pickup/putdown 动画）。

### 13.5 状态机行为细节

| 场景 | 状态转换 | 备注 |
|---|---|---|
| 物品进入主手（首次） | IDLE → PICKUP → HOLD | 第一次检查主手时只记录物品，不触发 pickup（避免上线时自动播放） |
| PICKUP 中按 U 键 | PICKUP → HOLD → DRINK | U 键排队 pendingDrink，PICKUP 播完进 HOLD 时自动触发 DRINK |
| HOLD 中按 U 键 | HOLD → DRINK | 立即触发，无需全局锁 |
| DRINK 播完 | DRINK → IDLE | 触发消耗包（C2S）扣物品 |
| HOLD 中切物品（如换成镐子） | HOLD → PUTDOWN → IDLE | 播放"放下"动画；新物品若有 pickup 配置则排队 |
| PICKUP/PUTDOWN 中切物品 | 当前态 → IDLE（强制 reset） | 动画中切物品，直接消失，不播 putdown |
| DRINK 中切物品 | DRINK 自然播完 → IDLE | 不 reset，DRINK 动画继续播到结束；切到的新物品若有 pickup 配置则排队 `pendingPickupItem`，等 DRINK 结束后触发 |
| PUTDOWN 进行中切到新饮料物品 | PUTDOWN 播完后才触发新物品 PICKUP | 新物品 PICKUP 排队 pendingPickupItem，等所有 handler IDLE 后触发 |
| 未配置 putdown 时切物品 | HOLD → IDLE（直接 reset） | 物品瞬间消失 |

### 13.6 调试技巧

1. **pickup 不播放**：检查 `registerPickup` 是否在 `register` 之后调用；检查 animation.json 内是否有 `pickupRawAnimName` 对应的动画
2. **HOLD 态骨骼不定格**：确认 pickup 动画在 BlockBench 内有结尾关键帧（`thenPlayAndHold` 定格最后一帧）；若 pickup 动画末尾骨骼回到原位，HOLD 态也会回到原位
3. **切物品时没有 putdown**：检查 `putdownRawAnimName` 是否为 null（null 表示不播下动画）；检查旧物品是否在 HOLD 态（PICKUP/PUTDOWN 态切物品会直接 reset，不播 putdown；DRINK 态切物品则让 DRINK 自然播完，不触发 putdown）
4. **切物品时新手 pickup 不触发**：可能是旧物品 putdown 还在播（全局锁），新物品 pickup 已排队 `pendingPickupItem`，等 putdown 播完后会自动触发；查看日志搜索 `[DrinkAnimDebug]` 确认状态
5. **PICKUP 中按 U 键后没反应**：U 键已排队，PICKUP 播完进 HOLD 时会自动触发 DRINK；如果 PICKUP 时长很短（如 0.3s），延迟几乎察觉不到

### 13.7 与 FirstPerson Model / Epic Fight 兼容性

主模组的 Mixin 已自动适配五阶段状态机：

- `ItemInHandRendererMixin` 调用 `FpArmRenderSystem.isDrinkPlaying()` 判断是否屏蔽原版双手渲染。`isDrinkPlaying()` 内部走 `DrinkAnimRegistry.anyPlaying()` → `h.isPlaying()` → `state.isActive()`，覆盖 PICKUP/HOLD/DRINK/PUTDOWN 全部状态
- `FirstPersonModelLogicHandlerMixin`（FirstPerson Model mod 兼容）同样通过 `isDrinkPlaying()` 判断，HOLD 态时强制隐藏 FirstPerson Model 自身手臂，避免与定格骨骼重叠
- `EpicFightRenderEngineMixin`（Epic Fight mod 兼容）通过 `isDrinkPlaying()` 取消 Epic Fight 的手部渲染

**HOLD 态时原版手中物品（如矿泉水）也会被屏蔽**：`ItemInHandRendererMixin` 在 `shouldRenderDrink()` 返回 true 时 `ci.cancel()`，原版主手物品渲染被取消，由 DrinkAnimHandler 用 GeckoLib 渲染定格骨骼 + 物品。

---

## 十四、获取帮助

- 发现 API bug 或需要新功能：联系 Realistic Dining 模组作者
- 本指南不清晰的地方：联系作者补充文档
- 附属生态链接收录：可将你的附属提交给作者列入官方推荐列表

---

